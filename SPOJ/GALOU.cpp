
#include "bits/stdc++.h"
using namespace std;

int n,m;
char grid[105][105];
inline char get(int r,int c)
{
  if(r<0 || r>n || c<0 || c>n )
    return '.';
  return grid[r][c];
}

//Make each one update the state
int updateState
//States ARE * B I C(cw) W(ccw)

//
//
//
//
//
//


int main()
{
  std::ios::sync_with_stdio(false);
  #ifndef ONLINE_JUDGE
    freopen("in.txt","r",stdin);
  #endif
  cin>>n>>m;
  while(!(n==0 && m==0))
  {

    cin>>n>>m;
  } 
}